#服务启动端口
server:
  port: 7100

# mysql
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/epoch?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&useSSL=false&autoReconnect=true&failOverReadOnly=false
    username: root
    password: 123456